import installer from "./defaults";
export * from "./install";
export * from "@datav/components";

export const install = installer.install;
export const version = installer.version;

export default installer;
